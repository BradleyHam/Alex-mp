import { ImCross } from "react-icons/im";

export default function WhyNotTories({points, heading}: {points: string[], heading: string}) {

    return (
        <div className="">
 
            {/* <div className="text-side lg:flex-1 max-w-3xl mx-auto ">
            <h2 className="text-3xl  mb-8 text-center">{heading}</h2>
               <ul className="flex flex-col space-y-6">
                    { points.map((point, index) =>  <div className="flex items-center space-x-4"><div className="w-[30px]"><ImCross size={25} color='#53212F'/></div>
<li className='text-lg' key={index}>{point}</li></div>)}
               </ul>
            </div> */}
   
    </div>
    )
}

